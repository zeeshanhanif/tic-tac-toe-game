import { defineConfig, devices } from "@playwright/test";

// UI/E2E smoke tests (ADR-006) over the architecture's critical flows (§8
// Testing). Thin happy-path coverage of the built app, run headless. Assert on
// roles/text, not pixels. `pretest:e2e` builds first; this previews the bundle.
export default defineConfig({
  testDir: "./tests/e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? "line" : "list",
  use: {
    baseURL: "http://localhost:4173",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run preview -- --port 4173 --strictPort",
    url: "http://localhost:4173",
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
});
