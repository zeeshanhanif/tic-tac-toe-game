# Scaffold Progress

> Statuses: pending | in-progress | done
> Last updated: 2026-07-14
> STATUS: COMPLETE — skeleton green locally; first deploy + first CI run pending.

## Phase 1–2 — Contract & gaps
- [done] Scaffold plan confirmed (unit: single SPA · Vanilla TS + Vite · npm · single-repo)
- [done] Gaps: package manager = npm; deploy target = Vercel (vercel.json + GH Actions CI)

## Phase 3 — Preflight & tooling
- [done] Preflight (node 22.18.0, npm 10.9.3, git 2.50.1; no Docker/DB needed)
- [done] Generator: create-vite 9.1.1 `--template vanilla-ts` (recorded in scaffold-notes)

## Phase 4 — Generation & structure
- [done] Generate Vite vanilla-ts app (temp dir → merged to root; temp removed)
- [done] core/ui/infra folders + ESLint no-restricted-imports boundary rules
- [done] Stripped demo boilerplate; docs/ already in place; CLAUDE.md written

## Phase 5 — Skeleton wiring
- [done] tokens.json → src/ui/generated/tokens.css (gen-tokens.mjs), consumed via var()
- [done] main.ts + shell view-switch (Setup/Game/Stats)
- [done] empty 3×3 board renders + click places mark (UI→core.applyMove→re-render)

## Phase 6 — Foundations
- [done] CI (.github/workflows/ci.yml: gen→lint→test→build) · vercel.json · dev logger · a11y base (focus, ≥44px)

## Phase 7 — Verification
- [done] clean `npm ci` + build + test(2/2) + lint — all green
- [done] boundary enforcement proven (core→ui import rejected)
- [done] token render verified in built CSS (defined + var() consumed + dark theme)
- [done] vercel.json valid JSON; CI YAML structurally valid; no secrets; doc paths resolve
- [pending] first deploy (Vercel) — user's step · first CI run — on first push
